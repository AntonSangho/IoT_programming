# 나만의 센서 데이터베이스 설계하기
**Arduino + DHT11 → MySQL 저장 구조 설계 실습**

이름: ___________________  날짜: ___________

---

### 🎯 학습 목표
- 저장할 데이터를 스스로 파악하고 테이블 구조를 설계할 수 있다.
- 적절한 데이터 타입을 선택하고 그 이유를 설명할 수 있다.
- PRIMARY KEY와 AUTO_INCREMENT의 필요성을 이해할 수 있다.
- CREATE TABLE SQL을 스스로 작성할 수 있다.

---

## STEP 1. 왜 데이터베이스가 필요할까?

지금까지 Flask로 Arduino 데이터를 웹에서 볼 수 있었어요. 그런데 한 가지 문제가 있습니다.

**Q1.** Flask 서버를 껐다가 다시 켜면 이전에 받은 센서 데이터는 어떻게 될까요?

```
답:
```

&nbsp;

**Q2.** 데이터를 "계속 기억"하려면 어디에 저장해야 할까요?

```
답:
```

> 💡 **힌트:** 데이터베이스는 서버가 꺼져도 데이터를 보관합니다. 마치 일기장에 매일 기록하는 것처럼요.

---

## STEP 2. 무엇을 저장할까? (컬럼 설계)

데이터베이스 테이블을 만들기 전에 '무엇을 저장할지' 먼저 생각해야 합니다.

**Q3.** DHT11 센서로 얻을 수 있는 데이터가 무엇인지 아래에 모두 적어보세요.

```
답:
```

&nbsp;

**Q4.** 데이터가 언제 측정됐는지도 저장해야 할까요? 그렇다면 왜 필요한가요?

```
답:
```

&nbsp;

**Q5.** 데이터가 1000개 쌓였을 때, 특정 하나를 찾으려면 무엇이 있어야 할까요?

```
답:
```

> 💡 **힌트:** 각 행을 구별할 수 있는 고유한 번호(ID)가 있으면 특정 데이터를 쉽게 찾을 수 있습니다.

---

## STEP 3. 어떤 형태로 저장할까? (데이터 타입)

MySQL에서는 저장하는 데이터 종류에 따라 타입을 정해야 합니다. 아래 표를 참고하세요.

| 타입 | 사용 예시 | 설명 |
|------|-----------|------|
| `INT` | 1, 2, 3, ... | 정수 (소수점 없음) |
| `FLOAT` | 25.3, 60.7, ... | 소수점 있는 숫자 |
| `VARCHAR(n)` | "쾌적", "더움" | 문자열 (최대 n자) |
| `DATETIME` | 2026-02-20 09:30:00 | 날짜 + 시간 |

&nbsp;

**Q6.** 온도(예: 25.3)와 습도(예: 60.5)는 어떤 데이터 타입이 적합한가요? 이유도 써보세요.

```
답:
```

&nbsp;

**Q7.** 측정 시간은 어떤 데이터 타입이 적합한가요?

```
답:
```

---

## STEP 4. 테이블 설계 완성하기

앞에서 생각한 내용을 바탕으로 아래 표를 완성해 보세요.

| 컬럼 이름 | 데이터 타입 | NULL 허용? | 설명 |
|-----------|-------------|------------|------|
| `id` | | NO | 각 행의 고유 번호 |
| | | | |
| | | | |
| | | | |

> 💡 **힌트:** `id` 컬럼에는 `INT AUTO_INCREMENT PRIMARY KEY` 를 사용합니다. 숫자가 자동으로 1씩 증가해요!

---

## STEP 5. CREATE TABLE SQL 작성 도전!

앞에서 설계한 내용을 바탕으로 아래 빈칸(`______`)을 채워 SQL을 완성해 보세요.

```sql
CREATE TABLE ______ (
    id        INT AUTO_INCREMENT PRIMARY KEY,
    ______    ______,          -- 온도
    ______    ______,          -- 습도
    ______    DATETIME DEFAULT CURRENT_TIMESTAMP  -- 측정 시간
);
```

✅ 완성됐다면 직접 MySQL에 입력해서 테이블이 만들어지는지 확인해 보세요!

---

## STEP 6. 저장된 데이터로 무엇을 알 수 있을까? (심화)

테이블에 데이터가 쌓이면 다양한 질문에 답할 수 있어요. 각 질문에 맞는 SQL을 생각해 보세요.

**Q8.** 온도가 28도 이상인 데이터만 보고 싶다면 어떤 SQL을 쓸까요?

```sql
-- 여기에 작성:
```

&nbsp;

**Q9.** 가장 높은 온도를 기록한 데이터를 찾으려면?

```sql
-- 여기에 작성:
```

&nbsp;

**Q10.** 오늘 날짜의 데이터만 뽑으려면 어떤 조건이 필요할까요?

```sql
-- 여기에 작성:
```

> 💡 **힌트:** `WHERE`, `MAX()`, `DATE()` 함수들이 도움이 됩니다. 모르면 선생님께 힌트 카드를 요청하세요!

---

## ✔ 오늘 수업 체크리스트

- [ ] 왜 데이터베이스가 필요한지 설명할 수 있다.
- [ ] 테이블 컬럼 4개를 스스로 설계했다.
- [ ] 데이터 타입을 적절히 선택하고 이유를 말할 수 있다.
- [ ] CREATE TABLE SQL을 완성하고 실행했다.
- [ ] (심화) SELECT 쿼리를 1개 이상 작성해봤다.
